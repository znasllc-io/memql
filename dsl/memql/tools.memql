// Generated by scripts/restructure-by-construct.
// Per-construct DSL layout: every <kind>s.memql file in a domain
// holds every construct of that kind in the domain. The companion
// files cover the other construct kinds.

@enabled
@handler(type="query", query="help({\"name\": \"$args.name\"})")
@executionTime("fast")
@description("Get detailed usage guidance for a MemQL named function (docs, args schema, and key calling rules)")
@mcp
tool describeFunction {
  name  string  @required @description("The function name to describe (e.g., activeSpaces)")
}

// ---- tools/v1/memql/toolSearchUsers.memql ----

// toolSearchUsers - Search for Users
// ====================================
//
// Search for users in the system.

// Pagination (epic #1964 / memql#1975): the dynamic `limit` bound is applied
// HERE by the handler's outer paginate($args.limit) wrapper. The inner
// querySearchUsers therefore declares ONLY `sort` (deterministic, keyset-
// eligible order) and NO inner `paginate` -- two nested paginate() directives
// are rejected by the engine ("multiple paginate() directives are not
// supported"), which broke this tool for every call after the 5.3 backfill
// added an inner `paginate 50` while this wrapper was still here. One paginate,
// driven by the caller's limit.
@enabled
@handler(type="query", query="paginate(querySearchUsers({active: $args.active}), $args.limit)")
@executionTime("fast")
@description("Search for users")
@mcp
tool searchUsers {
  active  boolean  @description("Filter by active status")
  limit   integer  @default("10") @description("Maximum number of results to return")
}

# CI routing verification (memql#856)

Throwaway file under dsl/ to exercise the dsl-only path filter. Not a .memql file, so it does not affect lint/engine-load/sdk-gen logic. Delete after verification.

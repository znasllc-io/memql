// Generated by scripts/restructure-by-construct.
// Per-construct DSL layout: every <kind>s.memql file in a domain
// holds every construct of that kind in the domain. The companion
// files cover the other construct kinds.

@row
@useConcept(policyTrace)
@description("Comprehensive policy-trace projection for v1:platform:policyTrace rows. Backs the policyTracesForPolicy debug query and any operator surface that lists recent policy evaluations.")
shape policyTraceFull {
  row.id
  policyTrace.policyName
  policyTrace.tier
  policyTrace.actorUserId
  policyTrace.actorRole
  policyTrace.callerPartition
  policyTrace.argsHash
  policyTrace.resultJson
  policyTrace.traceJson
  policyTrace.durationMs
  policyTrace.error
  row.createdAt
}

@row
@useConcept(partitionVariable)
@description("Comprehensive configuration variable projection. Used for both v1:platform:partitionVariable (partition-scoped) and v1:platform:globalVariable (global) -- the two concepts share the same field layout, so one shape serves both.")
shape variableFull {
  row.id
  partitionVariable.name
  partitionVariable.value
  partitionVariable.description
  row.createdAt
}

// Addresses for the Data origins surface (epic memql#4378).
//
// One page, no detail route: a domain is a (concept, connector) pair whose
// whole state fits in a row of the table, and a detail page would be a
// second address for the same eight numbers.

export const DATA_ORIGINS_ROOT = "/data-origins";
export const DATA_ORIGINS_ROUTE_PATTERN = "data-origins";

// Code generated by scripts/sdk-gen. DO NOT EDIT.
// Source of truth: dsl/**/*.memql.

import { QueryClient, type QueryCallOptions } from "./query.js";
import type { Result } from "./types.js";
import { renderMemQLValue } from "./memqlValue.js";

/** Daily 04:00 UTC sweep that ages out pending access requests older than IDENTITY_ACCESS_REQUEST_EXPIRY_DAYS (default 30) by stamping status='expired'. Per-row windowing via addDuration(createdAt, P{N}D) since MemQL filters can't push the duration math down. Pending queue is small (admin), so daily walk is cheap. */
export interface LogicAccessRequestExpirySweepArgs {
  event: Record<string, unknown>;
}

export function buildLogicAccessRequestExpirySweep(args: LogicAccessRequestExpirySweepArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicAccessRequestExpirySweep({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicAccessRequestExpirySweep(args: LogicAccessRequestExpirySweepArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicAccessRequestExpirySweep = function (this: QueryClient, args: LogicAccessRequestExpirySweepArgs = {} as LogicAccessRequestExpirySweepArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicAccessRequestExpirySweep", buildLogicAccessRequestExpirySweep(args), opts);
};

/** Daily 09:05 UTC sweep that emits an 'identity.deletion.reminder' event for each user 25-26 days into their deletion cooldown -- final notice before accountDeletionSweep performs the hard delete at 30 days. Bracket [P25D, P26D) on deletionScheduledAt fired daily means each user gets exactly one reminder at this milestone. */
export interface LogicAccountDeletionReminder25DaysArgs {
  event: Record<string, unknown>;
}

export function buildLogicAccountDeletionReminder25Days(args: LogicAccountDeletionReminder25DaysArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicAccountDeletionReminder25Days({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicAccountDeletionReminder25Days(args: LogicAccountDeletionReminder25DaysArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicAccountDeletionReminder25Days = function (this: QueryClient, args: LogicAccountDeletionReminder25DaysArgs = {} as LogicAccountDeletionReminder25DaysArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicAccountDeletionReminder25Days", buildLogicAccountDeletionReminder25Days(args), opts);
};

/** Daily 09:00 UTC sweep that emits an 'identity.deletion.reminder' event for each user 7-8 days into their deletion cooldown so they can cancel before the cooldown elapses. Bracket [P7D, P8D) on deletionScheduledAt fired daily means exactly one reminder per user at this milestone. Email delivery is consumed by the component/server subscriber. */
export interface LogicAccountDeletionReminder7DaysArgs {
  event: Record<string, unknown>;
}

export function buildLogicAccountDeletionReminder7Days(args: LogicAccountDeletionReminder7DaysArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicAccountDeletionReminder7Days({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicAccountDeletionReminder7Days(args: LogicAccountDeletionReminder7DaysArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicAccountDeletionReminder7Days = function (this: QueryClient, args: LogicAccountDeletionReminder7DaysArgs = {} as LogicAccountDeletionReminder7DaysArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicAccountDeletionReminder7Days", buildLogicAccountDeletionReminder7Days(args), opts);
};

/** Daily 03:00 UTC sweep that hard-deletes users whose deletionScheduledAt + IDENTITY_DELETION_COOLDOWN_DAYS (default 30) has elapsed, via mutationDeleteUserHard. Audit-event rows, invitations the user issued, and access-request rows are intentionally retained for the trail. */
export interface LogicAccountDeletionSweepArgs {
  event: Record<string, unknown>;
}

export function buildLogicAccountDeletionSweep(args: LogicAccountDeletionSweepArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicAccountDeletionSweep({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicAccountDeletionSweep(args: LogicAccountDeletionSweepArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicAccountDeletionSweep = function (this: QueryClient, args: LogicAccountDeletionSweepArgs = {} as LogicAccountDeletionSweepArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicAccountDeletionSweep", buildLogicAccountDeletionSweep(args), opts);
};

/** Daily 02:00 UTC sweep over v1:identity:auditEvent rows older than IDENTITY_AUDIT_LOG_RETENTION_DAYS. Observation-only today: emits an 'identity.audit.retention.observed' event with the candidate count -- MemQL has no delete() mutation and AuditEvent's append-only semantics forbid soft-delete via active=false. Per-row delete lands when one of those gaps closes. */
export interface LogicAuditEventRetentionSweepArgs {
  event: Record<string, unknown>;
}

export function buildLogicAuditEventRetentionSweep(args: LogicAuditEventRetentionSweepArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicAuditEventRetentionSweep({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicAuditEventRetentionSweep(args: LogicAuditEventRetentionSweepArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicAuditEventRetentionSweep = function (this: QueryClient, args: LogicAuditEventRetentionSweepArgs = {} as LogicAuditEventRetentionSweepArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicAuditEventRetentionSweep", buildLogicAuditEventRetentionSweep(args), opts);
};

/** Triggered when a v1:cognition:space is created with status='active'. Auto-joins the creator's General Assistant as a v1:cognition:participant on the new space, with forUserId=creator and isGroupGA=true (so the participant guard prevents non-elevated callers from removing it via the Roster tab). Idempotent. Emits 'si.auto-joined' for observability. Specialist auto-join is deferred until the runtime can iterate selectedAgentIds. */
export interface LogicAutoJoinSIArgs {
  event: Record<string, unknown>;
}

export function buildLogicAutoJoinSI(args: LogicAutoJoinSIArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicAutoJoinSI({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicAutoJoinSI(args: LogicAutoJoinSIArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicAutoJoinSI = function (this: QueryClient, args: LogicAutoJoinSIArgs = {} as LogicAutoJoinSIArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicAutoJoinSI", buildLogicAutoJoinSI(args), opts);
};

/** Bootstraps the cluster's self-representation on first startup. Creates v1:cluster:cluster + v1:cluster:database + v1:cluster:identityProvider rows when no cluster row exists. Only BFF nodes run the create path. Idempotent: skips entirely if a cluster row already exists. */
export interface LogicBootstrapClusterArgs {
  event: Record<string, unknown>;
}

export function buildLogicBootstrapCluster(args: LogicBootstrapClusterArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicBootstrapCluster({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicBootstrapCluster(args: LogicBootstrapClusterArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicBootstrapCluster = function (this: QueryClient, args: LogicBootstrapClusterArgs = {} as LogicBootstrapClusterArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicBootstrapCluster", buildLogicBootstrapCluster(args), opts);
};

/** Auto-creates a v1:cognition:session record when a v1:cognition:participant is created, with default device + stream state. Ensures every participant has a session tracking their real-time interaction state. Idempotent via queryParticipantSession existence check. Emits 'session.created' for downstream consumers. */
export interface LogicBootstrapSessionArgs {
  event: Record<string, unknown>;
}

export function buildLogicBootstrapSession(args: LogicBootstrapSessionArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicBootstrapSession({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicBootstrapSession(args: LogicBootstrapSessionArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicBootstrapSession = function (this: QueryClient, args: LogicBootstrapSessionArgs = {} as LogicBootstrapSessionArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicBootstrapSession", buildLogicBootstrapSession(args), opts);
};

/** Triggered on new v1:data:record creation with a non-empty naturalKeyValue. Queries confirmed records for matching natural key + record type; if any exist, emits 'data.conflicts.detected' event for the UI's ConflictResolutionPanel. Never auto-resolves -- always requires human approval. */
export interface LogicConflictDetectionArgs {
  event: Record<string, unknown>;
}

export function buildLogicConflictDetection(args: LogicConflictDetectionArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicConflictDetection({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicConflictDetection(args: LogicConflictDetectionArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicConflictDetection = function (this: QueryClient, args: LogicConflictDetectionArgs = {} as LogicConflictDetectionArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicConflictDetection", buildLogicConflictDetection(args), opts);
};

/** Records node shutdown by creating a v1:cluster:spawnEvent with action='stopped' and reason='system.shutdown'. Fires when the node is going down. */
export interface LogicDeregisterNodeArgs {
  event: Record<string, unknown>;
}

export function buildLogicDeregisterNode(args: LogicDeregisterNodeArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicDeregisterNode({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicDeregisterNode(args: LogicDeregisterNodeArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicDeregisterNode = function (this: QueryClient, args: LogicDeregisterNodeArgs = {} as LogicDeregisterNodeArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicDeregisterNode", buildLogicDeregisterNode(args), opts);
};

/** Ensure today's daily space exists for the authenticated caller. Top-level Execute entry point for interactive clients; resolves userId from the request's auth context inside the dailyspace integration. */
export interface LogicEnsureDailySpaceForCallerArgs {
}

export function buildLogicEnsureDailySpaceForCaller(args: LogicEnsureDailySpaceForCallerArgs): string {
  void args;
  return "logicEnsureDailySpaceForCaller({})";
}

declare module "./query.js" {
  interface QueryClient {
    logicEnsureDailySpaceForCaller(args?: LogicEnsureDailySpaceForCallerArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicEnsureDailySpaceForCaller = function (this: QueryClient, args: LogicEnsureDailySpaceForCallerArgs = {} as LogicEnsureDailySpaceForCallerArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicEnsureDailySpaceForCaller", buildLogicEnsureDailySpaceForCaller(args), opts);
};

/** Fires on every v1:identity:authSession create (one per login). Calls dailyspace.ensureForUser for the authenticating user; the idempotent insert collapses to a no-op when today's daily already exists. This is the path that makes the cron-frequency concern moot -- every login guarantees today's daily exists before the SPA reads. */
export interface LogicEnsureDailySpaceOnAuthSessionArgs {
  event: Record<string, unknown>;
}

export function buildLogicEnsureDailySpaceOnAuthSession(args: LogicEnsureDailySpaceOnAuthSessionArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicEnsureDailySpaceOnAuthSession({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicEnsureDailySpaceOnAuthSession(args: LogicEnsureDailySpaceOnAuthSessionArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicEnsureDailySpaceOnAuthSession = function (this: QueryClient, args: LogicEnsureDailySpaceOnAuthSessionArgs = {} as LogicEnsureDailySpaceOnAuthSessionArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicEnsureDailySpaceOnAuthSession", buildLogicEnsureDailySpaceOnAuthSession(args), opts);
};

/** Generates and inserts an SI response when the Go cognition handler decides a non-streaming response is needed and emits this event with pre-loaded prompt data. Calls the prompt template, inserts a v1:cognition:utterance via mutationSendTextUtterance, and bumps participant presence to idle. Idempotent via queryHasSIResponseForReply. */
export interface LogicGenerateResponseArgs {
  event: Record<string, unknown>;
}

export function buildLogicGenerateResponse(args: LogicGenerateResponseArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicGenerateResponse({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicGenerateResponse(args: LogicGenerateResponseArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicGenerateResponse = function (this: QueryClient, args: LogicGenerateResponseArgs = {} as LogicGenerateResponseArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicGenerateResponse", buildLogicGenerateResponse(args), opts);
};

/** Triggered on v1:identity:user update. When preferences.computerUseEnabled flips true->false, transitions every running plan owned by that user (with computerUseScope set) to awaitingFeedback with feedbackReason='kill_switch_engaged'. Re-enable flips the flag back; resume is per-plan-explicit (no auto-resume on flag flip). */
export interface LogicKillSwitchSuspendsRunningPlansArgs {
  event: Record<string, unknown>;
}

export function buildLogicKillSwitchSuspendsRunningPlans(args: LogicKillSwitchSuspendsRunningPlansArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicKillSwitchSuspendsRunningPlans({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicKillSwitchSuspendsRunningPlans(args: LogicKillSwitchSuspendsRunningPlansArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicKillSwitchSuspendsRunningPlans = function (this: QueryClient, args: LogicKillSwitchSuspendsRunningPlansArgs = {} as LogicKillSwitchSuspendsRunningPlansArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicKillSwitchSuspendsRunningPlans", buildLogicKillSwitchSuspendsRunningPlans(args), opts);
};

/** Hourly sweep that stamps consumedAt + consumedFromIP='system:expiry' on v1:identity:magiclink rows whose expiresAt is in the past, so the row reads as 'spent' in audit queries and any subsequent click is idempotently rejected by the consume handler's own expiresAt guard. */
export interface LogicMagicLinkExpirySweepArgs {
  event: Record<string, unknown>;
}

export function buildLogicMagicLinkExpirySweep(args: LogicMagicLinkExpirySweepArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicMagicLinkExpirySweep({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicMagicLinkExpirySweep(args: LogicMagicLinkExpirySweepArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicMagicLinkExpirySweep = function (this: QueryClient, args: LogicMagicLinkExpirySweepArgs = {} as LogicMagicLinkExpirySweepArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicMagicLinkExpirySweep", buildLogicMagicLinkExpirySweep(args), opts);
};

/** Emits a 'delegation.created' event when a new v1:identity:delegation row lands. Carries the delegation id + identity / agent / role / scope fields for audit logging and downstream event processing. */
export interface LogicOnDelegationCreatedArgs {
  event: Record<string, unknown>;
}

export function buildLogicOnDelegationCreated(args: LogicOnDelegationCreatedArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicOnDelegationCreated({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicOnDelegationCreated(args: LogicOnDelegationCreatedArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicOnDelegationCreated = function (this: QueryClient, args: LogicOnDelegationCreatedArgs = {} as LogicOnDelegationCreatedArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicOnDelegationCreated", buildLogicOnDelegationCreated(args), opts);
};

/** Fires when a new v1:identity:user row lands. Delegates to the dailyspace.ensureForUser capability which owns the timezone math + idempotent insert of today's daily space for that user. dailySpaceEnabled gating lives inside the Go capability since the value sits under preferences and filter syntax doesn't navigate nested payload paths reliably. */
export interface LogicProvisionDailySpaceOnUserCreateArgs {
  event: Record<string, unknown>;
}

export function buildLogicProvisionDailySpaceOnUserCreate(args: LogicProvisionDailySpaceOnUserCreateArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicProvisionDailySpaceOnUserCreate({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicProvisionDailySpaceOnUserCreate(args: LogicProvisionDailySpaceOnUserCreateArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicProvisionDailySpaceOnUserCreate = function (this: QueryClient, args: LogicProvisionDailySpaceOnUserCreateArgs = {} as LogicProvisionDailySpaceOnUserCreateArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicProvisionDailySpaceOnUserCreate", buildLogicProvisionDailySpaceOnUserCreate(args), opts);
};

/** Daily 02:00 UTC sweep that hard-deletes archived v1:cognition:space rows whose payload.expiresAt has passed (deadline pre-computed at archive time as archivedAt + retentionDays). Defensive: only acts on rows where status='archived' AND active=true AND expiresAt<now. Saved/scheduled/active spaces are untouched. */
export interface LogicPurgeExpiredArchivedSpacesArgs {
  event: Record<string, unknown>;
}

export function buildLogicPurgeExpiredArchivedSpaces(args: LogicPurgeExpiredArchivedSpacesArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicPurgeExpiredArchivedSpaces({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicPurgeExpiredArchivedSpaces(args: LogicPurgeExpiredArchivedSpacesArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicPurgeExpiredArchivedSpaces = function (this: QueryClient, args: LogicPurgeExpiredArchivedSpacesArgs = {} as LogicPurgeExpiredArchivedSpacesArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicPurgeExpiredArchivedSpaces", buildLogicPurgeExpiredArchivedSpaces(args), opts);
};

/** Daily 02:30 UTC sweep over v1:platform:policyTrace rows older than MEMQL_POLICYTRACE_RETENTION_DAYS (default 90). Currently observation-only: emits a 'platform.policyTrace.retention.observed' event with the candidate count. Per-row delete lands when the engine grows a delete() mutation. */
export interface LogicPurgeExpiredPolicyTracesArgs {
  event: Record<string, unknown>;
}

export function buildLogicPurgeExpiredPolicyTraces(args: LogicPurgeExpiredPolicyTracesArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicPurgeExpiredPolicyTraces({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicPurgeExpiredPolicyTraces(args: LogicPurgeExpiredPolicyTracesArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicPurgeExpiredPolicyTraces = function (this: QueryClient, args: LogicPurgeExpiredPolicyTracesArgs = {} as LogicPurgeExpiredPolicyTracesArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicPurgeExpiredPolicyTraces", buildLogicPurgeExpiredPolicyTraces(args), opts);
};

/** Reads candidate knowledge-domain rows via queryDueRefreshDomains; for each, the Go-side cron handler (registered by the planner-service init path) evaluates the elapsed-since-lastSeededAt check and spawns a trainSpecialist Plan per matching row. Per Q9 the schema-side cadence + runtime-side spawn are deliberately split -- the DSL filter is broad (active + non-null refreshCadenceDays), the precise elapsed-time math runs in Go where arithmetic on datetime fields is cheap. */
export interface LogicRefreshDueKnowledgeDomainsArgs {
  event: Record<string, unknown>;
}

export function buildLogicRefreshDueKnowledgeDomains(args: LogicRefreshDueKnowledgeDomainsArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicRefreshDueKnowledgeDomains({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicRefreshDueKnowledgeDomains(args: LogicRefreshDueKnowledgeDomainsArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicRefreshDueKnowledgeDomains = function (this: QueryClient, args: LogicRefreshDueKnowledgeDomainsArgs = {} as LogicRefreshDueKnowledgeDomainsArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicRefreshDueKnowledgeDomains", buildLogicRefreshDueKnowledgeDomains(args), opts);
};

/** Registers this node on startup. Creates a v1:cluster:node record (using the peer's NodeId as the explicit concept id so later NodeStatusWriter rows share the same time-series timeline) and a v1:cluster:spawnEvent with action='spawned' and reason='system.startup'. Runs on every node type; safe to re-run on every restart. */
export interface LogicRegisterNodeArgs {
  event: Record<string, unknown>;
}

export function buildLogicRegisterNode(args: LogicRegisterNodeArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicRegisterNode({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicRegisterNode(args: LogicRegisterNodeArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicRegisterNode = function (this: QueryClient, args: LogicRegisterNodeArgs = {} as LogicRegisterNodeArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicRegisterNode", buildLogicRegisterNode(args), opts);
};

/** On every v1:planner:plan update, checks the new status; if terminal (succeeded/failed/cancelled) AND a v1:workbench:workspace exists for the planId AND it is still provisioned, flips the concept row to released AND calls the workbench integration to remove the on-disk directory tree. Also fires the teardown unconditionally on terminal status even when no concept row exists -- the MVP Go integration provisions workspaces in-memory + on-disk without writing the concept row, so the teardown call cleans those up too. Idempotent on both paths. */
export interface LogicReleaseWorkspaceOnPlanTerminalArgs {
  event: Record<string, unknown>;
}

export function buildLogicReleaseWorkspaceOnPlanTerminal(args: LogicReleaseWorkspaceOnPlanTerminalArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicReleaseWorkspaceOnPlanTerminal({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicReleaseWorkspaceOnPlanTerminal(args: LogicReleaseWorkspaceOnPlanTerminalArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicReleaseWorkspaceOnPlanTerminal = function (this: QueryClient, args: LogicReleaseWorkspaceOnPlanTerminalArgs = {} as LogicReleaseWorkspaceOnPlanTerminalArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicReleaseWorkspaceOnPlanTerminal", buildLogicReleaseWorkspaceOnPlanTerminal(args), opts);
};

/** Find every expired active delegation and soft-revoke it with `system:expiry` as the revoker. Returns the count processed. */
export interface LogicRevokeExpiredDelegationsArgs {
  now: string;
}

export function buildLogicRevokeExpiredDelegations(args: LogicRevokeExpiredDelegationsArgs): string {
  const parts: string[] = [];
  parts.push("now: " + renderMemQLValue(args.now));
  return "logicRevokeExpiredDelegations({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicRevokeExpiredDelegations(args: LogicRevokeExpiredDelegationsArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicRevokeExpiredDelegations = function (this: QueryClient, args: LogicRevokeExpiredDelegationsArgs = {} as LogicRevokeExpiredDelegationsArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicRevokeExpiredDelegations", buildLogicRevokeExpiredDelegations(args), opts);
};

/** Hourly cron tick. Delegates the entire per-user sweep to the dailyspace.rolloverAllUsers capability, which walks queryActiveUsers and calls ensureForUser per row. Returns a one-row summary the automation logs. */
export interface LogicRolloverDailySpaceArgs {
  event?: Record<string, unknown>;
}

export function buildLogicRolloverDailySpace(args: LogicRolloverDailySpaceArgs): string {
  const parts: string[] = [];
  if (args.event !== undefined) parts.push("event: " + renderMemQLValue(args.event));
  return "logicRolloverDailySpace({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicRolloverDailySpace(args: LogicRolloverDailySpaceArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicRolloverDailySpace = function (this: QueryClient, args: LogicRolloverDailySpaceArgs = {} as LogicRolloverDailySpaceArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicRolloverDailySpace", buildLogicRolloverDailySpace(args), opts);
};

/** Phase 7 of chat-architecture. Triggered when a user's activeSpaceId pointer changes. When exactly two humans become active in the same space, emits a public 'voice.migrated.group' canvas card plus per-user 'voice.migrated.private' cards announcing voice transport migration from Team to Group thread. Idempotent via content-addressed stateIds. */
export interface LogicVoiceMigrationOnSecondHumanArgs {
  event: Record<string, unknown>;
}

export function buildLogicVoiceMigrationOnSecondHuman(args: LogicVoiceMigrationOnSecondHumanArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicVoiceMigrationOnSecondHuman({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicVoiceMigrationOnSecondHuman(args: LogicVoiceMigrationOnSecondHumanArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicVoiceMigrationOnSecondHuman = function (this: QueryClient, args: LogicVoiceMigrationOnSecondHumanArgs = {} as LogicVoiceMigrationOnSecondHumanArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicVoiceMigrationOnSecondHuman", buildLogicVoiceMigrationOnSecondHuman(args), opts);
};

/** Daily 02:30 UTC sweep that soft-deletes v1:worker:invocation rows older than WORKER_INVOCATION_RETENTION_DAYS (default 90). Per-row windowing via addDuration since MemQL filters can't push duration math down. Per-user invocation queries filter on @skipDeleted so rows disappear from UIs the moment the soft-delete lands. */
export interface LogicWorkerInvocationRetentionSweepArgs {
  event: Record<string, unknown>;
}

export function buildLogicWorkerInvocationRetentionSweep(args: LogicWorkerInvocationRetentionSweepArgs): string {
  const parts: string[] = [];
  parts.push("event: " + renderMemQLValue(args.event));
  return "logicWorkerInvocationRetentionSweep({" + parts.join(", ") + "})";
}

declare module "./query.js" {
  interface QueryClient {
    logicWorkerInvocationRetentionSweep(args: LogicWorkerInvocationRetentionSweepArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.logicWorkerInvocationRetentionSweep = function (this: QueryClient, args: LogicWorkerInvocationRetentionSweepArgs = {} as LogicWorkerInvocationRetentionSweepArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("logicWorkerInvocationRetentionSweep", buildLogicWorkerInvocationRetentionSweep(args), opts);
};


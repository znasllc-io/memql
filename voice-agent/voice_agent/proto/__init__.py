"""Generated protobuf stubs land here.

Run `make voice-agent` (which calls `scripts/voice-agent/install.sh`) to
generate `memql_pb2.py` + `memql_pb2_grpc.py` from
`component/grpc/memql.proto`. The stubs are .gitignored because they
are reproducible from the proto source.
"""

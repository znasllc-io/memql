{
  "name": "rebuild",
  "kind": "install",
  "description": "Rebuild this cluster's MemQL images from your source checkout and restart it on them.",
  "steps": [
    {
      "id": "rebuildFromCheckout",
      "script": "k3d.dev",
      "description": "Building MemQL from your source checkout, loading it into the cluster, and restarting the cluster on it.",
      "params": {
        "image-source": "checkout"
      },
      "dependsOn": [],
      "elevation": "none",
      "receipt": "rebuild",
      "preExistingPath": "none",
      "timeoutSeconds": 2700,
      "verify": {
        "kind": "resultEquals",
        "field": "result.imageSource",
        "value": "checkout"
      }
    }
  ]
}

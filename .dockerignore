.git
.github
bin
dist
*.test
*.out
coverage*
.claude
*.md
.env*
node_modules
**/node_modules
.DS_Store
deploy/k8s
# A host-built SPA bundle must never enter the build context: the image's
# bundle is produced by the Dockerfile's own spa stage, from source, and a
# stale dist/ copied in would be indistinguishable from a fresh one.
clients/os/dist

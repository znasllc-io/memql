/// The tickets that match, newest first.
query ticket matchingTicketsMaximum {
  args {
    priority  int  @maximum(5)
  }
  filter priority <= args.priority
  sort "row.createdAt", "desc"
  paginate 20
}

/// The caller's own open tickets, reading the actor without declaring it.
query ticket openTicketsActorUndeclared {
  filter row => row.ownerUserId == actor.userId && row.status == "open"
  sort "row.createdAt", "desc"
  paginate 20
}

/// The live tip of every open ticket, newest first: what it returns depends on when it is read.
@latestMode
query ticket openTicketsLatestMode {
  filter row => row.status == "open"
  sort "row.createdAt", "desc"
  paginate 20
  asOf latest
}

/// The open tickets, newest first, paginated and also declared unbounded.
@unbounded("the open-ticket queue is small enough to read whole")
query ticket openTicketsUnboundedPaginated {
  filter row => row.status == "open"
  sort "row.createdAt", "desc"
  paginate 20
}

/// The open tickets, newest first, for a caller holding read on principal.
@requiresCapability("read", "principal")
query ticket openTicketsRequiresCapability {
  filter status == "open"
  sort "row.createdAt", "desc"
  paginate 20
}

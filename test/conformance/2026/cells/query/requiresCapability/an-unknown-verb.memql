/// The open tickets, newest first, for a verb no role can hold.
@requiresCapability("inspect", "principal")
query ticket openTicketsUnknownVerb {
  filter status == "open"
  sort "row.createdAt", "desc"
  paginate 20
}

/// The open tickets, newest first, for server-side callers only.
@serverOnly(true)
query ticket openTicketsServerOnly {
  filter row => row.status == "open"
  sort "row.createdAt", "desc"
  paginate 20
}

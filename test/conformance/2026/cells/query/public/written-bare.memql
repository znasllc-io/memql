/// The open tickets, newest first. Deliberately callable with no caller check: the queue is public.
@public
query ticket openTicketsPublic {
  filter row => row.status == "open"
  sort "row.createdAt", "desc"
  paginate 20
}

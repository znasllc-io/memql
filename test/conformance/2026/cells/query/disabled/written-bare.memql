/// The open tickets, newest first. Switched off: not loaded while @disabled stays.
@disabled
query ticket openTicketsDisabled {
  filter row => row.status == "open"
  sort "row.createdAt", "desc"
  paginate 20
}

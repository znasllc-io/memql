/// The open tickets, newest first, for developers and anyone ranked above them.
@requiresRank(developer)
query ticket openTicketsRequiresRank {
  filter row => row.status == "open"
  sort "row.createdAt", "desc"
  paginate 20
}

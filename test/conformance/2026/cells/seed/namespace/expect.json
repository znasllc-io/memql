{
  "cases": [
    {
      "file": "a-string.memql",
      "verdict": "load_ok",
      "note": "Accepted, and read by nothing today: the seed converter keeps the value (seedDecl.namespace) and compileSeedDecl never passes it on -- a seed's concept namespace comes only from a file-top use import. This case pins that the form loads, not an effect."
    },
    {
      "file": "with-an-unquoted-value.memql",
      "verdict": "refuse_parse",
      "code": "annotation_form",
      "message": "@namespace on a seed takes one string"
    }
  ]
}

/// Attach files to a ticket, keeping the ones already attached.
@appendFields(attachmentIds)
mutation ticket attachToTicketAppendFields {
  args {
    ticketId       string!
    attachmentIds  []string!
  }
  update {
    id: args.ticketId
    attachmentIds: args.attachmentIds
  }
}

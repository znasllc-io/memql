/// Change some of a ticket's preferences, keeping the ones the caller did not name.
@mergeFields(preferences)
mutation ticket setTicketPreferencesMergeFields {
  args {
    ticketId     string!
    preferences  object!
  }
  update {
    id: args.ticketId
    preferences: args.preferences
  }
}

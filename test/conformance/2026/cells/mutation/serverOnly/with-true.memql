/// Retitle one ticket, for server-side callers only.
@serverOnly(true)
mutate ticket retitleTicketServerOnly {
  args {
    ticketId  string!
    title     string!
  }
  update {
    id: args.ticketId
    title: args.title
  }
}

/// Add labels to a ticket; a label it already carries is not added twice.
@addToSet("labels")
mutation ticket labelTicketAddToSet {
  args {
    ticketId  string!
    labels    []string!
  }
  update {
    id: args.ticketId
    labels: args.labels
  }
}

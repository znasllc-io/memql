/// Retitle one ticket, exposed as its own MCP tool.
@mcp
mutation ticket retitleTicketMcp {
  args {
    ticketId  string!
    title     string!
  }
  update {
    id: args.ticketId
    title: args.title
  }
}

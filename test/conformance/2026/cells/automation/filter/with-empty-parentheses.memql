/// When an agent node joins the cluster, open a ticket to welcome it.
@trigger(event="node.created", concept="v1:cluster:node")
@filter()
automation welcomeNodeFilter {
  raise := mutation raiseTicketFilter(title: "A node joined the cluster")
}

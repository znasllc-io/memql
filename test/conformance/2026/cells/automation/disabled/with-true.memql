/// When a node joins the cluster, open a ticket. Switched off: not loaded while @disabled stays.
@trigger(event="node.created", concept="v1:cluster:node")
@disabled(true)
automation welcomeNodeDisabled {
  raise := mutation raiseTicketDisabled(title: "A node joined the cluster")
}

@trigger(event="node.created", concept="v1:cluster:node")
@description("When a node joins the cluster, open a ticket to welcome it.")
automation welcomeNodeDescription {
  raise := mutation raiseTicketDescription(title: "A node joined the cluster")
}

/// When a node joins the cluster, open a ticket to welcome it.
@trigger(event="node.created", concept="v1:cluster:node", partition="*")
automation welcomeNodeTrigger {
  step raise {
    mutation raiseTicketTrigger (title: "A node joined the cluster")
  }
}

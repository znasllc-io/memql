/// A work-spine template that also fires on every node that joins.
@template
@trigger(event="node.created", concept="v1:cluster:node")
automation raiseRequestedTicketTriggered {
  args {
    /// The ticket's title.
    title  string!
  }

  raise := mutation raiseTicketTemplate(title: args.title)
}

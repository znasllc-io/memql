/// When a node joins the cluster, open a ticket -- two modes, so a reader cannot tell which governs.
@trigger(event="node.created", concept="v1:cluster:node")
@mode(single, queued)
automation welcomeNodeMode {
  raise := mutation raiseTicketMode(title: "A node joined the cluster")
}

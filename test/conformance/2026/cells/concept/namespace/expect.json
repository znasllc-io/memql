{
  "cases": [
    {
      "file": "a-string.memql",
      "verdict": "load_ok",
      "note": "A concept's namespace defaults to its directory; one that is neither the directory nor <directory>:... loads only when the directory's namespace.pin names it (#2614). The pin sits beside the cases here, and the runner mounts it into the case's domain."
    },
    {
      "file": "with-an-unquoted-value.memql",
      "verdict": "refuse_load",
      "code": "annotation_form",
      "message": "@namespace on a concept takes one string"
    },
    {
      "file": "a-namespace-the-pin-does-not-name.memql",
      "verdict": "refuse_load",
      "message": "@namespace(\"helpdesk\") does not match its domain directory",
      "note": "The directory's namespace.pin names support; a namespace neither the directory nor its pin names would silently move every canonical id (#2614)."
    }
  ]
}

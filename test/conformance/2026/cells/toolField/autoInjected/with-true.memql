/// List the open tickets.
@handler(type="query", query="query ticketsToListAutoInjected()")
tool listTicketsAutoInjected {
  requestedBy  string  @autoInjected(true)  @description("The agent asking; stamped by the runtime, never by the model.")
}

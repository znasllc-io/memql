/// List the tickets, newest first.
@handler(type="query", query="paginate(query ticketsToListRequired(status: $args.status), 20)")
tool listTicketsRequired {
  status  string  @required(true)  @description("Which tickets to list.")
}

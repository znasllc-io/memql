/// List the tickets, newest first.
@handler(type="query", query="paginate(query ticketsToListDescription(), args.limit)")
tool listTicketsDescription {
  limit  integer  @description("The most tickets to return.")
}

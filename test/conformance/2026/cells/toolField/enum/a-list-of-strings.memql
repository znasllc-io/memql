/// List the tickets, newest first.
@handler(type="query", query="paginate(query ticketsToListEnum(status: $args.status), 20)")
tool listTicketsEnum {
  status  string  @enum("open", "closed")  @description("Which tickets to list.")
}

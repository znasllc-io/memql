use capabilities.fs.{ readFile }

/// Read a ticket's attachment from the runner's workspace, with @enabled written twice.
@enabled
@enabled
action readTicketAttachmentEnabledTwice {
  args {
    path  string!
  }
  capability readFile(path: args.path)
}

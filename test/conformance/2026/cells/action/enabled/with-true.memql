use capabilities.fs.{ readFile }

/// Read a ticket's attachment from the runner's workspace. @enabled restates the default.
@enabled(true)
action readTicketAttachmentEnabled {
  args {
    path  string!
  }
  capability readFile(path: args.path)
}

/// Note a goal, filtered with the retired now().
@trigger(event="node.updated", concept="v1:work:goal")
@filter(row => row.closedAt < now())
automation tfNowCall {
  args {
    id  string
  }
  step note {
    mutation tfNoteGoal(goalId: args.id, note: "changed")
  }
}

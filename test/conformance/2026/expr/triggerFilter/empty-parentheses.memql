/// When a node joins the cluster, raise a ticket -- with a filter that says nothing.
@trigger(event="node.created", concept="v1:cluster:node")
@filter()
automation welcomeNodeUnfiltered {
  raise := mutation raiseFilteredTicket(title: "A node joined the cluster")
}

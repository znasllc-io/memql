/// Note a goal, filtered with the retired concat().
@trigger(event="node.updated", concept="v1:work:goal")
@filter(row => concat(row.status, "!") == "closed!")
automation tfConcatCall {
  args {
    id  string
  }
  step note {
    mutation tfNoteGoal(goalId: args.id, note: "changed")
  }
}

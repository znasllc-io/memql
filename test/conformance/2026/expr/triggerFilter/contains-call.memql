/// Note a goal, filtered with the retired contains(s, sub).
@trigger(event="node.updated", concept="v1:work:goal")
@filter(row => contains(row.statement, "outage"))
automation tfContainsCall {
  args {
    id  string
  }
  note := mutation tfNoteGoal(goalId: args.id, note: "changed")
}

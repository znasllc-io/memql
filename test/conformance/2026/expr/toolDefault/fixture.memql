/// A support ticket, the row the tool here lists.
concept ticket {
  title   string
  status  string
}

/// The open tickets, newest first.
query ticket openTicketsForTool {
  filter status == "open"
  sort "row.createdAt", "desc"
  paginate 20
}

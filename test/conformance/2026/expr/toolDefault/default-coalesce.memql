/// List the tickets, with a coalesced default.
@handler(type="query", query="paginate(query tdTickets(), args.limit)")
tool tdCoalesce {
  limit  integer  @default(args.limit ?? 10) @description("How many tickets to return.")
}

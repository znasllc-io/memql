/// List the tickets, with a comparison for a default.
@handler(type="query", query="paginate(query tdTickets(), args.limit)")
tool tdComparison {
  limit  integer  @default(10 == 10) @description("How many tickets to return.")
}

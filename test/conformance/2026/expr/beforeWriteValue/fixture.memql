/// A support ticket: the row the mutations here write.
concept ticket {
  title     string
  status    string
  priority  int
  tags      []string
}

/// Tickets that are open.
trait bwValueIsOpen = row => row.status == "open"

/// When a node joins the cluster, raise a ticket if it is an agent node.
@trigger(event="node.created", concept="v1:cluster:node")
automation welcomeAgentNodes {
  kind := logic joinedNodeType(event: event)
  if kind == "agent" {
    raise := mutation raiseAgentTicket(title: "An agent node joined the cluster")
  }
}

/// Write a note about a goal.
mutate goalNote acNoteGoal {
  args {
    goalId  string  @required
    note    string
  }
  insert {
    id:      args.goalId
    goalId:  args.goalId
    note:    args.note ?? ""
  }
}

/// Note a goal when it closes.
@trigger(event="node.updated", concept="v1:work:goal")
automation acNoteClosedGoal {
  args {
    id      string
    status  string
  }
  if args.status == "closed" {
    note := mutation acNoteGoal(goalId: args.id, note: "closed")
  }
}

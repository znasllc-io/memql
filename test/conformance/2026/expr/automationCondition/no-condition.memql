/// When a node joins the cluster, raise a ticket -- behind an `if` with nothing to decide.
@trigger(event="node.created", concept="v1:cluster:node")
automation welcomeNodeUnconditionally {
  kind := logic joinedNodeType(event: event)
  if  {
    raise := mutation raiseAgentTicket(title: "A node joined the cluster")
  }
}

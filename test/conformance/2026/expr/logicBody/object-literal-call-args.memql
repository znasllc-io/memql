/// How many tickets there are, with the call's arguments wrapped in an object literal.
logic lbObjectLiteralCall {
  args {
    status  string
  }
  rows := query lbTickets({ status: args.status })
  return rows.count()
}

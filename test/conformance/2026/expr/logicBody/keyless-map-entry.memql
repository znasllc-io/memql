/// The payload announcing a new delegation, with one entry written without
/// its key.
logic lbKeylessMapEntry {
  args {
    event  object  @required
  }
  return { delegationId: args.event.payload.id, args.event.payload.identityId, timestamp: now }
}

/// Note a goal, with the call's arguments wrapped in an object literal.
@trigger(event="node.updated", concept="v1:work:goal")
automation saObjectLiteralCall {
  args {
    id  string
  }
  mutation saNoteGoal({ goalId: args.id })
}

/// A work-spine template: raises a ticket with the title the run names.
@template
automation raiseNamedTicket {
  args {
    /// The ticket's title.
    title  string!
  }

  raise := mutation raiseTicketFromStep(title: args.title)
}

# =============================================================================
# WIP (#616): Blue/green BFF topology -- DRAFT, NOT WIRED INTO kustomization.yaml
# =============================================================================
#
# This is the design-draft replacement for the single `bff` Deployment in
# bff.yaml. It is intentionally NOT added to kustomization.yaml yet -- it ships
# alongside bff.yaml so the owner can review the shape and approve the
# production cutover before it goes live. (decision 8: the cutover stays a
# separate operator step; it is NOT folded into aks-deploy.sh.)
#
# GOAL (distinct from #615 graceful-drain, which already shipped):
#   - Two BFF colors run simultaneously (bff-blue + bff-green).
#   - NEW user logins land ONLY on the ACTIVE color (the user-facing entry
#     Services -- bff-active ClusterIP behind the nginx /memql Ingress, and
#     bff-external LoadBalancer -- select on memql/color=<active>).
#   - EXISTING bidirectional MemqlService.Stream sessions keep hitting the OLD
#     color until they close (the OLD color's pods are not killed at cutover;
#     they keep serving their already-open streams).
#   - The OLD color is removed only AFTER it has drained to 0 active streams
#     (polled via /healthz activeStreams, the #616 primitive) or a max-drain
#     deadline -- at which point the OLD color's pods take the #615 graceful
#     -drain teardown (preStop + GOAWAY + terminationGracePeriod).
#
# SCOPE (decision 1): BFF-only. Mesh nodes (cognition/agent/planner/voice) are
# stateless behind the BFF and roll normally once the BFF drains; voice/LiveKit
# room drain is a separate story, out of scope here.
#
# WHY the in-mesh `bff` Service stays color-agnostic:
#   Peer nodes dial `bff:50058` (NodeService) for cross-node forwards. Those
#   forwards must reach WHICHEVER color holds the user's stream, so the in-mesh
#   `bff` Service selects BOTH colors. Only the USER-facing entry (8085 via
#   Ingress + the external LB) is color-pinned, because that is where "new
#   login -> new color" is enforced.
#
# Cutover is driven by scripts/deploy/bff-bluegreen-cutover.sh (operator step).
#
# -----------------------------------------------------------------------------
# TOPOLOGY (decision 5): two explicit color Deployments (below). The duplication
# is explicit-but-verbose; a kustomize base + per-color overlay is DRYer but
# hides the color in patch files. Two explicit Deployments chosen for review
# clarity now; an overlay refactor can come later without changing behavior.
# -----------------------------------------------------------------------------

# ---- BLUE color Deployment ---------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bff-blue
  namespace: memql
  labels:
    app.kubernetes.io/name: bff
    app.kubernetes.io/part-of: memql
    memql/node-type: bff
    memql/color: blue
spec:
  # Each color carries the full replica count. During a cutover BOTH colors are
  # briefly up. The #614 cluster autoscaler is LIVE (min 2 / max 5 on B2s) and
  # supplies the surge headroom (decision 7). The cutover script scales the OLD
  # color DOWN progressively as it drains (default), so peak is bounded and we
  # do NOT sit at a sustained 2x for the whole drain window.
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: bff
      memql/color: blue
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bff
        app.kubernetes.io/part-of: memql
        memql/node-type: bff
        memql/color: blue
    spec:
      volumes:
        - name: memql-ca
          secret:
            secretName: memql-ca
      # #615 graceful-drain knobs are unchanged -- they govern the eventual
      # teardown of whichever color is being retired.
      terminationGracePeriodSeconds: 60
      containers:
        - name: bff
          image: acrmemql.azurecr.io/memql-bff-copresent:0.9.6
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
          ports:
            - { name: http, containerPort: 8085 }
            - { name: grpc, containerPort: 50051 }
            - { name: node, containerPort: 50058 }
          envFrom:
            - secretRef:
                name: memql-secrets
          env:
            - { name: MEMQL_GENESIS_AUTOLOAD, value: "true" }
            - { name: SERVICE_NAME, value: "memQL" }
            - { name: MEMQL_NODE_TYPE, value: "bff" }
            - { name: MEMQL_NODE_ADDRESS, value: "bff:50058" }
            - { name: MEMQL_NODE_SERVICE_ADDRESS, value: ":50058" }
            - { name: MEMQL_WORKER_PEERS, value: "voice=voice:50059,agent=agent:50055,cognition=cognition:50054,planner=planner:50056,workbench=workbench:50060" }
            - { name: MEMQL_GRPC_ADDRESS, value: ":50051" }
            - { name: SERVER_ADDRESS, value: "0.0.0.0:8085" }
            - { name: IDENTITY_VERIFIER_BASE_URL, value: "https://identity:8085" }
            - { name: IDENTITY_VERIFIER_AUDIENCE, value: "memql" }
            - { name: IDENTITY_VERIFIER_EXPECTED_ISSUER, value: "https://identity.staging.copresent.ai" }
            - { name: IDENTITY_ENABLED, value: "false" }
            - { name: MEMQL_HTTP_TLS_CA_FILE, value: "/etc/memql/cacerts/ca.crt" }
            - { name: SSL_CERT_DIR, value: "/etc/ssl/certs:/etc/memql/cacerts" }
            - { name: SERVER_ALLOWED_ORIGINS, value: "*" }
            - { name: MEMORY_NODES_DATABASE_MIGRATE_ON_START, value: "false" }
            - { name: MEMORY_NODES_DATABASE_AUTO_MIGRATE, value: "false" }
          volumeMounts:
            - { name: memql-ca, mountPath: /etc/memql/cacerts, readOnly: true }
          readinessProbe:
            httpGet: { path: /healthz, port: 8085 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8085 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 200m, memory: 256Mi }
---
# ---- GREEN color Deployment --------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bff-green
  namespace: memql
  labels:
    app.kubernetes.io/name: bff
    app.kubernetes.io/part-of: memql
    memql/node-type: bff
    memql/color: green
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: bff
      memql/color: green
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bff
        app.kubernetes.io/part-of: memql
        memql/node-type: bff
        memql/color: green
    spec:
      volumes:
        - name: memql-ca
          secret:
            secretName: memql-ca
      terminationGracePeriodSeconds: 60
      containers:
        - name: bff
          image: acrmemql.azurecr.io/memql-bff-copresent:0.9.6
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
          ports:
            - { name: http, containerPort: 8085 }
            - { name: grpc, containerPort: 50051 }
            - { name: node, containerPort: 50058 }
          envFrom:
            - secretRef:
                name: memql-secrets
          env:
            - { name: MEMQL_GENESIS_AUTOLOAD, value: "true" }
            - { name: SERVICE_NAME, value: "memQL" }
            - { name: MEMQL_NODE_TYPE, value: "bff" }
            - { name: MEMQL_NODE_ADDRESS, value: "bff:50058" }
            - { name: MEMQL_NODE_SERVICE_ADDRESS, value: ":50058" }
            - { name: MEMQL_WORKER_PEERS, value: "voice=voice:50059,agent=agent:50055,cognition=cognition:50054,planner=planner:50056,workbench=workbench:50060" }
            - { name: MEMQL_GRPC_ADDRESS, value: ":50051" }
            - { name: SERVER_ADDRESS, value: "0.0.0.0:8085" }
            - { name: IDENTITY_VERIFIER_BASE_URL, value: "https://identity:8085" }
            - { name: IDENTITY_VERIFIER_AUDIENCE, value: "memql" }
            - { name: IDENTITY_VERIFIER_EXPECTED_ISSUER, value: "https://identity.staging.copresent.ai" }
            - { name: IDENTITY_ENABLED, value: "false" }
            - { name: MEMQL_HTTP_TLS_CA_FILE, value: "/etc/memql/cacerts/ca.crt" }
            - { name: SSL_CERT_DIR, value: "/etc/ssl/certs:/etc/memql/cacerts" }
            - { name: SERVER_ALLOWED_ORIGINS, value: "*" }
            - { name: MEMORY_NODES_DATABASE_MIGRATE_ON_START, value: "false" }
            - { name: MEMORY_NODES_DATABASE_AUTO_MIGRATE, value: "false" }
          volumeMounts:
            - { name: memql-ca, mountPath: /etc/memql/cacerts, readOnly: true }
          readinessProbe:
            httpGet: { path: /healthz, port: 8085 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8085 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 200m, memory: 256Mi }
---
# ---- In-mesh Service (COLOR-AGNOSTIC) ---------------------------------------
# Cross-node forwards (peers dialing bff:50058 / grpc 50051) must reach the
# color holding the user's stream, so this Service selects BOTH colors. It does
# NOT carry memql/color in its selector. NodeService routing + AiForward rely on
# this resolving to any healthy bff pod regardless of color.
apiVersion: v1
kind: Service
metadata:
  name: bff
  namespace: memql
  labels:
    app.kubernetes.io/name: bff
    app.kubernetes.io/part-of: memql
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bff
  ports:
    - { name: node, port: 50058, targetPort: 50058 }
    - { name: http, port: 8085, targetPort: 8085 }
    - { name: grpc, port: 50051, targetPort: 50051 }
---
# ---- User-facing entry Service (COLOR-PINNED) -------------------------------
# This is the cutover anchor. The nginx /memql Ingress points at THIS Service
# (bff-active) instead of the color-agnostic bff Service, so NEW user logins
# land only on the active color. The cutover flips spec.selector.memql/color
# from old -> new (the "Service selector flip"). Existing streams already bound
# to OLD-color pods keep flowing because the TCP/WS connection is already
# established to a specific pod; only NEW connection establishment is steered.
apiVersion: v1
kind: Service
metadata:
  name: bff-active
  namespace: memql
  labels:
    app.kubernetes.io/name: bff
    app.kubernetes.io/part-of: memql
    memql/role: user-entry
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bff
    memql/color: green   # <-- ACTIVE color. Flipped by the cutover script.
  ports:
    - { name: http, port: 8085, targetPort: 8085 }
    - { name: grpc, port: 50051, targetPort: 50051 }
---
# ---- External LoadBalancer (COLOR-PINNED) ----------------------------------
# Same color pin as bff-active. New external connections land on the active
# color only. (decision 6: keep bff-external LB color-pinned to the active
# color; the public entry path is nginx Ingress -> bff-active, and this LB is
# the color-pinned external fallback. Both flip together in the cutover script.)
apiVersion: v1
kind: Service
metadata:
  name: bff-external
  namespace: memql
  labels:
    app.kubernetes.io/name: bff
    app.kubernetes.io/part-of: memql
    memql/role: external-entry
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: bff
    memql/color: green   # <-- ACTIVE color. Flipped by the cutover script.
  ports:
    - { name: http, port: 8085, targetPort: 8085 }

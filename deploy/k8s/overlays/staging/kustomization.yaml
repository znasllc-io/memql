# Staging overlay -> 0.9.35. All 7 node images + the copresent SPA REBUILT from
# current main (memql 56c802d / memql-bff-copresent 0636e33 / copresent 3397bdf).
# Mesh-robustness release on top of 0.9.34's no-dup/no-drop end state:
#   - #1245 (every node): the forwarder no longer buffers+TTL-expires reply
#     events to dead-for-delivery peers (old blue-green / unready / never-dialed
#     pods). Makes rollouts converge cleanly instead of dropping replies during
#     the post-roll window. Builds on #1217 (gate) + #1232 (outbox).
#   - #114 (carrier DSL): canonical-id @pattern now allows camelCase concepts
#     (canvasState/...), so mutationCreateCanvasState stops rejecting the
#     space.created card.
#   - copresent: Simli-only avatar picker (#418) on top of the prior batch.
# cognition/agent/planner/workbench/bff CARRIER-built; voice + identity engine.
# livekit pin (#1043) preserved.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: memql

resources:
  - ../../base

# Pin the media IP LiveKit advertises to the livekit-rtc LoadBalancer's public
# IP (#1043). Per-env because the LB IP differs by cluster. If the livekit-rtc
# Service is ever recreated and Azure assigns a new IP, update this value (or
# reserve a static IP — see docs/deploy/livekit-provision.md).
patches:
  - target:
      kind: Deployment
      name: livekit
    patch: |
      - op: add
        path: /spec/template/spec/containers/0/env/-
        value:
          name: NODE_IP
          value: "20.246.247.180"

images:
  # 0.9.38 — voice STT bootstrap falls back MEMQL_SI_OPENAI_API_KEY ->
  # OPENAI_API_KEY (#1371) so the audio websocket registers on the voice node;
  # planner claim race (#1369), Gate-1 phased headline (#1366).
  - name: acrmemql.azurecr.io/memql-identity
    digest: sha256:088829e671fddedf97c7632658ef51f8e307030ab5329033f517c6326625948d
  # Carrier-built; 0.9.38.
  - name: acrmemql.azurecr.io/memql-bff-copresent
    digest: sha256:345bbb881e9031dc5490ecbb8638b9566b00554e87d1f2794ef8f8d4bab0d74e
  # Carrier-built; 0.9.38.
  - name: acrmemql.azurecr.io/memql-cognition
    digest: sha256:46a357e54db1dc0e81f9eb1ddec7b83ee0c38508819d9ea08004cd46ef665e6d
  - name: acrmemql.azurecr.io/memql-voice
    digest: sha256:0eea86b881ddf977f5d7e3308fa2de6ed1df89683ddbf076f8dbf71cf162de46
  # Carrier-built; 0.9.38.
  - name: acrmemql.azurecr.io/memql-agent
    digest: sha256:cccdc4397fd1b0adae1d7ff9968c402784c61d183b9f7c901689d1321dc48721
  # Carrier-built; 0.9.38.
  - name: acrmemql.azurecr.io/memql-planner
    digest: sha256:2789c55830d4141061512555beb2b9072fb503d4e4bf09f02b2edfaf60085491
  # Carrier-built; 0.9.38.
  - name: acrmemql.azurecr.io/memql-workbench
    digest: sha256:456516eef203068451f879845241426ac26b735c134db5083769218eebb1550f
  # SPA rebuilt from copresent main (85fa33a) at 0.9.38 (coherence rebuild).
  - name: acrmemql.azurecr.io/copresent
    digest: sha256:6cbc24552fff4e34939013ef6fbf7ca2a0e58d1d23e089d8368a53662a12a166
  # Self-hosted LiveKit server (memql#1043) — upstream OSS image, digest-pinned.
  - name: livekit/livekit-server
    newName: livekit/livekit-server
    digest: sha256:2c6869d2d5ff6c9c0166f47be1c92dad6928bfecfa5e4060a6ece48db8accfa3

# Staging overlay -> 0.9.33. All 7 node images + the copresent SPA REBUILT from
# current main (memql fecdbb5 / memql-bff-copresent 2fa2778 / copresent 949163e).
# Carries on top of 0.9.32:
#   - memql: duplicate-utterance fix — advisory-lock dispatch gate so exactly one
#     cognition replica handles a turn (#1217); Anam avatar render-model + Sofia
#     persona fixes (#1209/#1219); local cluster parity tooling (#1212, dev-only,
#     no staging behavior change — the os.Hostname() node-id fallback is a no-op
#     where MEMQL_NODE_ID is set via fieldRef)
#   - copresent: persona-thumbnail same-origin (#403), Anam audio 24kHz (#401),
#     assistant Voice column align + randomize-name (#398/#399)
# cognition/agent/planner/workbench/bff CARRIER-built (memql-bff-copresent/
# Dockerfile + BUILD_TAGS); voice + identity engine builds. livekit pin (#1043)
# preserved.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: memql

resources:
  - ../../base

# Pin the media IP LiveKit advertises to the livekit-rtc LoadBalancer's public
# IP (#1043). Per-env because the LB IP differs by cluster. If the livekit-rtc
# Service is ever recreated and Azure assigns a new IP, update this value (or
# reserve a static IP — see docs/deploy/livekit-provision.md).
patches:
  - target:
      kind: Deployment
      name: livekit
    patch: |
      - op: add
        path: /spec/template/spec/containers/0/env/-
        value:
          name: NODE_IP
          value: "20.246.247.180"

images:
  - name: acrmemql.azurecr.io/memql-identity
    digest: sha256:73ae6c7afbc817d61965320b5951d6553e22ffa12d80a1929a0f908eb7d6079c
  # Carrier-built; 0.9.33 — adds the #1217 dispatch gate.
  - name: acrmemql.azurecr.io/memql-bff-copresent
    digest: sha256:2179d543eb8a660c46e069de7ef7f28b01830a3e2c741a4f1aac6c843fc477e8
  # TEMPORARY ROLLBACK to the 0.9.32 (pre-#1217) cognition build. The #1217
  # exactly-once dispatch gate is CORRECT but exposed #1232 (cross-node
  # graph-event delivery silently drops to churning peers -> intermittent
  # missing replies); the prior double-dispatch was masking it. Reverting just
  # cognition restores the redundancy (reliable replies; the dup returns) until
  # #1232 lands, at which point #1217 is re-enabled on top. All other nodes stay
  # 0.9.33. Wire-compatible (no memql.proto change between the builds).
  - name: acrmemql.azurecr.io/memql-cognition
    digest: sha256:ca99343d416d2ed2d74c018bdce2eb41217ac1a8db18f3d6caa649953ea7b1e3
  - name: acrmemql.azurecr.io/memql-voice
    digest: sha256:c133d4b2181d979b7b923bf985bb8f528bd65d6b7e45fccd9becb93757172f27
  # Carrier-built (engine + CoPresent DSL) -- #1053.
  - name: acrmemql.azurecr.io/memql-agent
    digest: sha256:aa85a3e0802b131b53534c82313cabfa006162e24c54a9ef14c98ff9dea65d79
  # Carrier-built (engine + CoPresent DSL #1053); 0.9.33.
  - name: acrmemql.azurecr.io/memql-planner
    digest: sha256:c25188387c3ff65a3e96333bfc1e6de4f14bfce71ecca2be6db4860dc9625510
  # Carrier-built (engine + CoPresent DSL) -- #1053.
  - name: acrmemql.azurecr.io/memql-workbench
    digest: sha256:5f33f2cd6e25ce6876ef0fafab083f94838200ff1f51c7eb0b3767dc62bce57c
  # SPA rebuilt from copresent main (949163e) -- #398/#399/#401/#403 on top of the 0.9.32 batch.
  - name: acrmemql.azurecr.io/copresent
    digest: sha256:8222ca16ea72ef3f64e373fe2567b1187e534c000622e01e4f00a3157176f03a
  # Self-hosted LiveKit server (memql#1043) — upstream OSS image, digest-pinned.
  - name: livekit/livekit-server
    newName: livekit/livekit-server
    digest: sha256:9e34703b97ceb9f622bcbb533107e4786c7aa65966f0494966a452ad41a0c0d4

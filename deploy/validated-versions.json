{
  "_schema": "Promotion ledger (memql#628). A version is appended here ONLY after a green DEEP staging smoke gate (SMOKE_PROFILE=deep). Prod deploys promote ONLY versions listed here, pinned by the recorded per-engine digests. Written by scripts/deploy/aks-deploy.sh record_validated_version; commit after each validated staging deploy. 0.9.6 is intentionally absent: it was rebuilt in-place (mutable tag) and never passed a deep gate.",
  "validated": []
}
